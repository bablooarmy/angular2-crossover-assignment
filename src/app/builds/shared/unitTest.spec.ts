/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import { UnitTest } from './unitTest';

describe('UnitTest', () => {
  it('should create an instance', () => {
    expect(new UnitTest()).toBeTruthy();
  });
});
